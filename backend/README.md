# Backend - Django REST API

User authentication backend with JWT tokens and MySQL database.

## Run Locally
```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
